[project]
name = "mini-apollo"
version = "0.1.0"
description = "A lightweight Apollo Config SDK for Python."
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.12.15",
    "requests>=2.32.4",
]
